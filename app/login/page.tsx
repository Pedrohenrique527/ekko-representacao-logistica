"use client";

import { zodResolver } from "@hookform/resolvers/zod";
import { useForm } from "react-hook-form";
import { z } from "zod";
import { ArrowRight, BarChart3, Eye, EyeOff, LockKeyhole, Mail, Moon, ShieldCheck, Sun } from "lucide-react";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";

const schema = z.object({ email: z.email("Informe um e-mail válido."), password: z.string().min(6, "A senha deve ter ao menos 6 caracteres."), remember: z.boolean() });
type FormValues = z.infer<typeof schema>;

export default function LoginPage() {
  const [show, setShow] = useState(false);
  const [light, setLight] = useState(false);
  const router = useRouter();
  const { register, handleSubmit, formState: { errors, isSubmitting } } = useForm<FormValues>({ resolver: zodResolver(schema), defaultValues: { email: "", password: "", remember: true } });
  useEffect(() => { const saved = localStorage.getItem("ekko-theme") === "light"; setLight(saved); document.documentElement.dataset.theme = saved ? "light" : "dark"; }, []);
  const toggleTheme = () => { const next = !light; setLight(next); document.documentElement.dataset.theme = next ? "light" : "dark"; localStorage.setItem("ekko-theme", next ? "light" : "dark"); };
  return <main className="grid min-h-screen bg-[#090a0c] lg:grid-cols-[1.08fr_.92fr]">
    <section className="relative hidden overflow-hidden border-r border-white/[.07] bg-[#0d0e10] p-12 lg:flex lg:flex-col lg:justify-between">
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_15%,rgba(31,93,80,.25),transparent_35%),radial-gradient(circle_at_75%_80%,rgba(190,157,92,.13),transparent_38%)]"/>
      <div className="relative z-10 flex items-center gap-4"><div className="grid size-16 place-items-center rounded-2xl bg-white p-2 shadow-2xl"><img src="/ekko-logo.png" alt="Ekko Revestimentos" className="h-full w-full object-contain"/></div><div><p className="text-lg font-semibold text-white">Ekko Representação Logística</p><p className="mt-1 text-[10px] uppercase tracking-[.18em] text-emerald-300">Business Intelligence</p></div></div>
      <div className="relative z-10 max-w-xl"><span className="inline-flex items-center gap-2 rounded-full border border-emerald-500/20 bg-emerald-500/10 px-3 py-1.5 text-[10px] font-semibold uppercase tracking-[.12em] text-emerald-300"><BarChart3 size={13}/>Ekko Revestimentos</span><h1 className="mt-6 text-5xl font-semibold leading-[1.08] tracking-[-.045em] text-white">Gestão inteligente de pedidos de representação.</h1><p className="mt-5 max-w-lg text-sm leading-6 text-zinc-400">Uma plataforma exclusiva para transformar a operação logística da Ekko em indicadores confiáveis, auditoria e decisões gerenciais.</p><div className="mt-10 grid grid-cols-3 gap-3">{[["Excel","preservado"],["PostgreSQL","dados reais"],["Auditoria","rastreável"]].map(([value,label])=><div key={label} className="rounded-xl border border-white/[.07] bg-white/[.035] p-4 backdrop-blur"><p className="text-sm font-semibold text-zinc-100">{value}</p><p className="mt-1 text-[10px] uppercase tracking-[.08em] text-zinc-500">{label}</p></div>)}</div></div>
      <p className="relative z-10 flex items-center gap-2 text-[10px] text-zinc-500"><ShieldCheck size={13}/>Ambiente corporativo protegido e auditado</p>
    </section>
    <section className="relative flex items-center justify-center p-6 sm:p-12"><button onClick={toggleTheme} aria-label="Alternar tema" className="absolute right-6 top-6 grid size-10 place-items-center rounded-xl border border-white/10 bg-white/[.04] text-zinc-400">{light?<Moon size={17}/>:<Sun size={17}/>}</button><div className="w-full max-w-[410px]"><div className="mb-9 flex items-center gap-3"><div className="grid size-14 place-items-center rounded-2xl bg-white p-2 shadow-xl"><img src="/ekko-logo.png" alt="Ekko" className="h-full w-full object-contain"/></div><div><p className="text-sm font-semibold text-zinc-100">Ekko Representação Logística</p><p className="mt-1 text-[9px] uppercase tracking-[.16em] text-emerald-400">Gestão inteligente de pedidos</p></div></div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-emerald-400">Acesso corporativo</p><h2 className="mt-3 text-3xl font-semibold tracking-tight text-white">Bem-vindo</h2><p className="mt-2 text-sm text-zinc-500">Entre para acessar os dados operacionais da Ekko Revestimentos.</p><form onSubmit={handleSubmit(async()=>{await new Promise(r=>setTimeout(r,250));router.push("/");})} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-xs font-medium text-zinc-400">E-mail</span><div className="relative"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600" size={16}/><input {...register("email")} className="h-12 w-full rounded-xl border border-white/10 bg-[#0d0e10] pl-10 pr-3 text-sm text-white outline-none focus:border-emerald-500/60 focus:ring-2 focus:ring-emerald-500/10"/></div>{errors.email&&<span className="mt-1.5 block text-[11px] text-rose-400">{errors.email.message}</span>}</label><label className="block"><span className="mb-2 block text-xs font-medium text-zinc-400">Senha</span><div className="relative"><LockKeyhole className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600" size={16}/><input type={show?"text":"password"} {...register("password")} className="h-12 w-full rounded-xl border border-white/10 bg-[#0d0e10] pl-10 pr-11 text-sm text-white outline-none focus:border-emerald-500/60 focus:ring-2 focus:ring-emerald-500/10"/><button type="button" aria-label={show?"Ocultar senha":"Mostrar senha"} onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-zinc-600">{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div>{errors.password&&<span className="mt-1.5 block text-[11px] text-rose-400">{errors.password.message}</span>}</label><label className="flex items-center gap-2 text-xs text-zinc-500"><input type="checkbox" {...register("remember")} className="size-4 accent-emerald-700"/>Lembrar-me</label><Button className="h-12 w-full bg-emerald-700 hover:bg-emerald-600" disabled={isSubmitting}>{isSubmitting?"Validando acesso...":<>Entrar<ArrowRight size={16}/></>}</Button></form><p className="mt-8 text-center text-[10px] text-zinc-600">© Ekko Revestimentos • Versão 1.0.0</p></div></section>
  </main>;
}
